version: '3.8'
services:
  frontend-dev:
    # Reemplaza con tu usuario y el nombre de tu repo
    image: ${DOCKER_USERNAME}/${DOCKER_IMAGE}:${IMAGE_TAG:-latest}
    container_name: react_frontend_dev
    restart: always
    ports:
      - "3000:80"