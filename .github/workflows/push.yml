name: Deploy Project on Server
on:
  workflow_run:
    workflows: ["Build and push Docker images"]
    types:
      - completed
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Start deploy.
        run: echo "Start deploy..."